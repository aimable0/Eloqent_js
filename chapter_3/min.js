/* a function that returns the minimum of the two arguments passed to it. */
function minimum(a, b) {
    return (a < b) ? a : b;
}

console.log(minimum(10, 11));
console.log(minimum(20, 11));
console.log(minimum(10, 31));
